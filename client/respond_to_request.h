#include <message.h>
#include <client_database.h>
#ifndef RESPOND_TO_REQUEST_H
#define RESPOND_TO_REQUEST_H

string send_details(map<string, torrent_for_map > &details_of_file, vector<string> &request);

#endif